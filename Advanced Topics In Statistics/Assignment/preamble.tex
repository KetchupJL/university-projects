\usepackage{graphicx}  % For including images
\usepackage{booktabs}  % For professional-looking tables
\usepackage{caption}  % For better figure and table captions
\usepackage{listings}  % For code formatting
\usepackage{hyperref}  % For clickable links and bookmarks (load last)
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

\lstset{
    basicstyle=\ttfamily\small,  % Use a smaller font for code
    columns=flexible,           % Adjust column spacing
    breaklines=true,            % Allow line breaks
    showspaces=false,           % Don’t show spaces
    showstringspaces=false,     % Don’t show spaces in strings
    lineskip=-1pt               % Reduce line spacing (make it tighter)
}